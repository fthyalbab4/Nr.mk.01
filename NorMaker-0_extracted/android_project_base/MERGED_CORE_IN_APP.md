# NOR Maker 0.1 — Merged Native Core

This Android project contains the current native core under `app/src/main/cpp` and the core validation evidence under `app/src/main/assets/core_validation`.

The latest source changes are included in the project itself. A new APK was attempted after the merge, but Gradle stopped before compilation because no Android SDK is installed or configured in this environment. Do not treat any older APK in the parent directory as containing this merge.
